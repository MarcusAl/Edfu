- content_for :title do
  Lessons
  \|
  = @lesson.course.title
  \|
  = @lesson.title

.container.mt-3
  .card
    .card-header
      %h1 Edit lesson
    .card-body
      = render 'form'

  = link_to 'Show', course_lesson_path(@course, @lesson), class: "btn btn-sm btn-secondary mt-3"
  %a{href: "javascript:history.back()", class: "btn btn-sm btn-outline-secondary mt-3"} Back